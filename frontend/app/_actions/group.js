"use server"

/*
    state = {
        error : "for single message error"
        errors : "for form fields errors"
        message : "for success message"
        data : "for returning data (exp grp component) "
    }
*/

export async function addGroupAction(prevState, formData) {

    // return state with data containing the grp component created to add it dinamically
}

export async function addGroupPostAction(prevState, formData) {

    // return state with data containing the post component created to add it dinamically
}

export async function addGroupEventAction(prevState, formData) {

    // return state with data containing the event component created to add it dinamically
}

